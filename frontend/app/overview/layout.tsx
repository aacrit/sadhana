/**
 * Overview layout — standalone, no app chrome.
 * Bypasses the main layout's fonts/tokens for a clean investor-facing page.
 */

export default function OverviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
